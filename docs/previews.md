# Model inspection

Use the [interactive viewer](viewer.md) on the documentation site. The earlier video and frame export tools have been removed.

The standalone HTML source viewer in `python/preview/build_viewer.py` remains available for mesh bundles. Geometry comparison images and numerical audit reports are retained under `docs/previews/`.
